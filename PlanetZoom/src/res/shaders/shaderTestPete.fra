#version 410 
#pragma debug(on)
#pragma optimize(off)

uniform sampler2D s;
smooth in vec2 uv;
in vec4 color_out;
in vec2 uv_out;

out vec4 out_Color;
 
void main(void)
{
	out_Color = (color_out + texture(s, uv_out)) / 2.0;
}
